# Your portrait

Drop your photo here as `me.jpg` (a square crop of your face works best).

The 3D character's face is a circular plane that maps this image as a texture.
If the file is missing, a generated placeholder silhouette is used instead, so
the site keeps working until you add your real photo.

Path referenced in code: `src/data/portfolioData.js` -> `profile.photo`.
